- name: Debug secret
  run: |
    if [ -z "$CLOUDFLARE_API_TOKEN" ]; then
      echo "TOKEN IS EMPTY ❌"
      exit 1
    else
      echo "TOKEN EXISTS ✅"
    fi
  env:
    CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}